name: Generate AI Insight

on:
  schedule:
    - cron: '0 0,12 * * *'  # 12시간마다 (UTC 00:00, 12:00 → KST 09:00, 21:00)
  workflow_dispatch:  # 수동 실행 가능

permissions:
  contents: write

concurrency:
  group: ai-insight
  cancel-in-progress: false

jobs:
  generate-insight:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Pull latest changes
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git pull origin main --rebase || true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Check cache file
        id: check-cache
        run: |
          if [ -f "data-cache.json" ]; then
            echo "cache_exists=true" >> $GITHUB_OUTPUT
            echo "📂 캐시 파일 존재 확인"
          else
            echo "cache_exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ 캐시 파일 없음 - 크롤링 실행"
          fi

      - name: Run crawling (if no cache)
        if: steps.check-cache.outputs.cache_exists == 'false'
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          FIRECRAWL_API_KEY: ${{ secrets.FIRECRAWL_API_KEY }}
        run: |
          echo "🕷️ 크롤링 시작..."
          node generate-html-report.js
          echo "✅ 크롤링 완료"
        timeout-minutes: 30

      - name: Generate AI Insight
        run: |
          echo "🤖 AI 인사이트 생성 시작..."
          node generate-ai-insight.js
        timeout-minutes: 15

      - name: Move reports to docs
        run: |
          mkdir -p docs/reports
          if [ -d "reports" ]; then
            cp -r reports/*.json docs/reports/ 2>/dev/null || true
            cp -r reports/*.html docs/reports/ 2>/dev/null || true
          fi

      - name: Commit and push
        run: |
          git add reports/ docs/reports/ docs/index.html docs/styles.css index.html styles.css 2>/dev/null || true
          if git diff --staged --quiet; then
            echo "📝 변경사항 없음"
          else
            git commit -m "AI Insight $(date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ AI 인사이트 푸시 완료"
          fi
